export default defineAppConfig({
  ui: {
    colors: {
      primary: 'lime',
      neutral: 'zinc'
    },
    button: {
      defaultVariants: {
        // Sleeker default buttons to match the glass aesthetic
      }
    }
  }
})
